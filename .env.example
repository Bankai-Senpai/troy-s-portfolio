# Optional server-side integration settings for a future API layer.
# Keep secrets private and never prefix them with VITE_ or NEXT_PUBLIC_.
OPENROUTER_API_KEY=
N8N_WEBHOOK_URL=
N8N_WEBHOOK_SECRET=
GITHUB_TOKEN=

